apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-api.fullname" . }}-headless
  labels: {{- include "report-api.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "report-api.selectorLabels" . | nindent 4 }}
