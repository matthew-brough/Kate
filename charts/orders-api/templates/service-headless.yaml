apiVersion: v1
kind: Service
metadata:
  name: {{ include "orders-api.fullname" . }}-headless
  labels: {{- include "orders-api.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "orders-api.selectorLabels" . | nindent 4 }}
