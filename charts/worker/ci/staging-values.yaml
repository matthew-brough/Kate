image:
  tag: ""  # Promotion must set this to an immutable git SHA.

pdb:
  enabled: true
  minAvailable: 1

networkPolicy:
  enabled: true

# keda disabled in staging — worker runs as a fixed single replica
