apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaos-portal.fullname" . }}
  labels: {{- include "chaos-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "chaos-portal.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
